require 'spec_helper'

RSpec.describe Nio4r::Websocket do
  it 'has a version number' do
    expect(Nio4r::Websocket::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
